# [SystemInstruction](https://solana-labs.github.io/solana-web3.js/classes/SystemInstruction.html)
> 系统指令

和 StakeInstruction 的区别？
 
```s
Methods

# 解码 分配 系统指令并检索指令参数。
# Decode an allocate system instruction and retrieve the instruction params.
decodeAllocate

# 通过种子系统指令解码 allocate 并检索指令参数。
# Decode an allocate with seed system instruction and retrieve the instruction params.
decodeAllocateWithSeed

# 解码分配系统指令并检索指令参数。
# Decode an assign system instruction and retrieve the instruction params.
decodeAssign

# 解码带有种子的赋值系统指令并检索指令参数。
# Decode an assign with seed system instruction and retrieve the instruction params.
decodeAssignWithSeed

# 解码创建帐户系统指令并检索指令参数。
# Decode a create account system instruction and retrieve the instruction params.
decodeCreateAccount

# 解码通过种子系统指引创建的账户，并检索指令参数
# Decode a create account with seed system instruction and retrieve the instruction params.
decodeCreateWithSeed

# 解码系统指令，并检索指令类型
# Decode a system instruction and retrieve the instruction type.
decodeInstructionType

# 解码临时预付款系统指令并检索指令参数。
# Decode a nonce advance system instruction and retrieve the instruction params.
decodeNonceAdvance

# 解码临时授权系统指令并检索指令参数
# Decode a nonce authorize system instruction and retrieve the instruction params.
decodeNonceAuthorize

# 解码临时初始化系统指令并检索指令参数
# Decode a nonce initialize system instruction and retrieve the instruction params.
decodeNonceInitialize

# 解码临时撤销系统指令并检索指令参数
# Decode a nonce withdraw system instruction and retrieve the instruction params.
decodeNonceWithdraw

# 解码临时转移系统指令并检索指令参数
# Decode a transfer system instruction and retrieve the instruction params.
decodeTransfer

# 解码转移的种子系统指令并检索指令参数
# Decode a transfer with seed system instruction and retrieve the instruction params.
decodeTransferWithSeed
```